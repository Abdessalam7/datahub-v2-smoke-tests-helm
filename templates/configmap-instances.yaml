apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "datahub-v2-smoke-tests.fullname" . }}-instances
  labels:
    app.kubernetes.io/name: {{ include "datahub-v2-smoke-tests.name" . }}
    helm.sh/chart: {{ printf "%s-%s" .Chart.Name .Chart.Version }}
data:
  instances.json: |
{{ .Values.smokeTest.instancesJson | indent 4 }}
